{
  "name": "obsidian-arc-web",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Frontend for the Obsidian Arc server. Built by Vite, embedded into the Go binary.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "jsdom": "^30.0.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^5.0.0"
  }
}
